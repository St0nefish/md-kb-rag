/target
/data
.env
config.yaml
deploy/deploy.env
*.db
*.db-journal
*.db-wal
*.db-shm
.claude/*
# Track shared project config (e.g. worktree provisioning); ignore everything
# else under .claude/ (local settings, worktrees, plugin caches).
!.claude/settings.json
.serena/
