# Webhook HMAC secret (shared with Gitea/GitHub/GitLab)
WEBHOOK_SECRET=changeme

# MCP server bearer token
MCP_BEARER_TOKEN=changeme

# Embedding model path (for bundled llama.cpp server)
MODEL_PATH=/path/to/models
MODEL_FILE=nomic-embed-text-v2-moe.gguf

# Knowledge base path (bind-mount mode)
KB_PATH=./data/repo
